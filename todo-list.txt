Analysis:
- Add backend check for local failsafe models (light/heavy GGUF) presence and optional auto-download.
- Provide SerpAPI failure fallback messaging/context so synthesis still succeeds.
- Update README with local model download instructions and fallback behavior.

Research:
- Referenced Hugging Face hub download guidance (hf_hub_download, local_dir_use_symlinks=False) for deterministic local copies.

Implementation:
- Extend LocalIntelligenceClient with ensure_models_available(check/download) and use before fallback.
- Enhance vision search flow to include SerpAPI failure context instead of silent skip.
- Document manual download steps (hf_hub_download snippet and huggingface-cli example) in README.

Testing:
- Run python -m pytest.

Documentation:
- README section for local failsafe models and SerpAPI fallback notes.
